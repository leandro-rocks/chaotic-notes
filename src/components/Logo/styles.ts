import { colors, typography } from "@styles";
import styled from "styled-components";

export const Text = styled.span`
  font-size: ${typography.sizing.xxlarge};
  font-weight: ${typography.weight.bold};
  color: ${colors.neutral.darkest};
`;
